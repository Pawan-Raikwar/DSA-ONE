public class DSA3 {
    public static void main(String[] args) {
        String newStr=" ";

        for(int i=0;i<newStr.length();i++){
            Integer count=1;

            while(i<newStr.length()-1 && newStr.charAt(i)==newStr.charAt(i+1)){
                    count++;
                    i++;
            }
             newStr+=newStr.charAt(i);
             if(count>1){
                newStr+=cout/true
             }
        }
    }
}
